const Data = new Map();
module.exports = {
	name: "voiceStateUpdate",
	async execute(oldGuild, newGuild, client) {
// 		setInterval(() => {
// 			if (newGuild.me.voice.channel.members.filter(m => !m.user.bot).size === 0) {
// 				Data.set("zero", Data.get("zero") + 1);
// 			} else {
// 				Data.set("zero", 0);
// 			}
// 			if (Data.get("zero") >= 300) {
// 				client.distube.stop("836791390100258897");
// 			}
// 		}, 1000);
	}
}
